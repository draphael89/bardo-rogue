# Rejected candidates

`pnpm art reject <candidate.png> --reason <why>` copies a rejected candidate here with a
hash-verified receipt. Pass `--manifest <run.manifest.json>` when available so the generation request
survives with the rejected pixels.

Rejection is production evidence, not approval. Files in this directory never compile into the game.
